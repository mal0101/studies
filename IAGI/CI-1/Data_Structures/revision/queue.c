#include <stdio.h>
#include <stdlib.h>

// all operations for queues are if constant time
// we can have array based implementations + linked list based implementations

// Array based implementation

